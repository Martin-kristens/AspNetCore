@{
    var user = await UserManager.GetUserAsync(User);
}

<header>
    <div class="container">
        <a id="logo" asp-controller="Home" asp-action="Index"><img src="~/images/solid.svg" alt="Logo for silicon"></a>

        <div id="menu" class="hide">
            <nav>
                <a class="menu-link" asp-controller="Home" asp-action="Index" asp-fragment="overview">Overview</a>
                <a class="menu-link" asp-controller="Home" asp-action="Index" asp-fragment="feature">Features</a>
                <a class="menu-link" asp-controller="Contact" asp-action="Index">Contact</a>

                @if (User.Identity!.IsAuthenticated)
                {
                    <a class="menu-link" asp-controller="Course" asp-action="Index">Courses</a>
                }

            </nav>
        </div>

        <div class="btn-switch">
            <label>Light</label>
            <label class="switch" for="switch-mode">
                <input type="checkbox" id="switch-mode" />
                <span class="slider round"></span>
            </label>
            <label>Dark</label>
        </div>

        <div id="account-buttons" class="account-buttons hide">
            @if (User.Identity.IsAuthenticated)
            {
                <a id="signout" class="btn-gray" asp-controller="Auth" asp-action="SignOut"><i class="fa-solid fa-arrow-right-from-bracket"></i>Sign Out</a>
                <div>
                    <a asp-controller="Account" asp-action="Details"><img class="profile-img" src="~/uploads/@user.ProfileImage" alt="profile image" /></a>
                </div>

            }
            else
            {
                <a class="btn-gray" asp-controller="Auth" asp-action="SignIn"><i class="fa-solid fa-right-to-bracket"></i>Sign In</a>
                <a class="btn-theme" asp-controller="Auth" asp-action="SignUp"><img src="../icons/head-body-icon.svg">Sign Up</a>
            }
        </div>

        <div id="mobile-menu"></div>

        <!-- Top Navigation Menu -->
        <button onclick="toggleMenu()" class="btn-mobile">
            <i class="fa-solid fa-bars-staggered"></i>
        </button>

        @*   <div class="btn-mobile" href="javascript:void(0);" onclick="toggleMenu()">
        <i class="fa-solid fa-bars-staggered"></i>
        </div>
        <div class="mobile-container">
        </div> *@

    </div>
</header>